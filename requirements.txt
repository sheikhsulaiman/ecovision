# EcoVision — LOCAL environment (this machine, VS Code).
#
# Scope: GEE scripting, preprocessing, boundary prep, reference-sample
# handling, evaluation, area estimation, figures. NOT deep-learning
# training — that lives in requirements-kaggle.txt. See CLAUDE.md,
# "Compute environment: Kaggle".
#
#   python -m venv venv
#   venv\Scripts\activate          # Windows
#   pip install -r requirements.txt

# Core geospatial
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
earthengine-api>=0.1.400
geemap>=0.32

# Traditional ML — the RF baseline (E1/E2) runs on CPU, no GPU needed
scikit-learn>=1.3
xgboost>=2.0

# Data handling
numpy>=1.26
pandas>=2.1

# Visualisation
matplotlib>=3.8
seaborn>=0.13
folium>=0.15
