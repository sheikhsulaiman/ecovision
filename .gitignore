# Raw and derived data — never commit. The `/*` form (not `/`) is required
# so the .gitkeep negation below works: git cannot re-include a file whose
# parent DIRECTORY is excluded. Keeping the .gitkeep means a fresh clone
# still has the folder, so scripts writing here don't hit FileNotFoundError.
data/raw/*
!data/raw/.gitkeep
data/patches/*
!data/patches/.gitkeep
data/pixels/
*.tif
*.tiff
*.npy

# data/reference/ is deliberately NOT ignored. The reference-sample
# interpretation CSVs are produced by hand, by both authors, over weeks —
# no script can regenerate them. They are the only non-derived data in this
# repo and the sole basis of every accuracy figure. Commit them.

# Rendered maps are derived AND large (GeoTIFF, often >100 MB — GitHub's
# per-file hard limit). Regenerate from src/.
outputs/maps/*
!outputs/maps/.gitkeep

# outputs/figures/ and outputs/tables/ ARE tracked, deliberately. They are
# derived, but small, and versioning them gives an audit trail of how every
# reported number moved as the pipeline changed. See CLAUDE.md.

# data/splits/ is tracked for a stronger reason than size. The block
# assignment defines what "test set" means for EVERY experiment. It is
# reproducible from a fixed seed, but it also depends on Hansen loss counts
# — so a Hansen version bump would silently reassign blocks, and two models
# trained a month apart would be evaluated on different test sets while
# appearing comparable. Freezing the assignment in git is what stops that.
# Regenerate deliberately with src/splits.py --write, never incidentally.

# Environment
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.env

# OS
.DS_Store

# Derived review chips — 1.6 GB of PNGs, regenerable from the candidate
# polygons. Keep the review CSV, not the imagery.
tearesults/chips/

# Interpretation aids, both regenerable from src/interpret_tool.py: the
# chip cache, and the ~30 MB self-contained HTML pages built from it. What
# must be committed is the interpreter's OUTPUT — the exported CSVs land in
# data/reference/, which is tracked.
data/interpret_cache/
outputs/interpret/

# The Kaggle upload bundle — ~840 MB of the same patches already ignored
# above, rezipped. Rebuild with src/package_kaggle.py.
outputs/kaggle/

# Model weights — ~90 MB each and reproducible from the patches plus a
# fixed seed. The metrics they produce are what gets committed.
outputs/checkpoints/

# Whatever was downloaded back from a Kaggle session. It is the staged
# copy of the inputs plus ~90 MB checkpoints; the RESULTS are copied out
# of it into outputs/tables/, and those are what belong in git.
outputs/kaggle-output*/
